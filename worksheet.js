let defintion = 4;

function developYourGoal(defintionNumber){
    let needSomethingDeveloped = true;
    while (needSomethingDeveloped = true)
    if (definitionNumber == 4 && hire == "y"){
       needSomethingDeveloped = false;
       console.log("got you covered")
    }
}

let hire = prompt("Congrats on filling out the contact information\n"+
  "press y to confirm.");

developYourGoal(definition);